# The studio refuses to store tokens without this.
TOKEN_ENCRYPTION_KEY=change-me-to-something-random

# Shared with the fake platform so it can sign delivery callbacks.
PLATFORM_WEBHOOK_SECRET=webhook-secret
